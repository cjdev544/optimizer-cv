{
  "name": "optimizer-cv",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "build:extension": "npm run build:extension --workspace frontend",
    "dev:extension": "npm run dev:extension --workspace frontend"
  }
}
